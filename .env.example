VITE_API_URL=http://localhost:8080

# Data source for the board. When 'true', the app loads boards/groups/parity from the
# backend's GET /board on startup (falling back to the local demo board on error).
# When 'false' or unset (default), the board uses the built-in localStorage demo data.
VITE_USE_BACKEND=false
